import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RoleRolesPermissionsComponent } from './role-roles-permissions.component';

describe('RoleRolesPermissionsComponent', () => {
  let component: RoleRolesPermissionsComponent;
  let fixture: ComponentFixture<RoleRolesPermissionsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RoleRolesPermissionsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RoleRolesPermissionsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
