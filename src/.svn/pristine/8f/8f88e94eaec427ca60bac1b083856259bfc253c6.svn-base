import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StreetMappingComponent } from './street-mapping.component';

describe('StreetMappingComponent', () => {
  let component: StreetMappingComponent;
  let fixture: ComponentFixture<StreetMappingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StreetMappingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StreetMappingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
