import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AreawiseOutComponent } from './areawise-out.component';

describe('AreawiseOutComponent', () => {
  let component: AreawiseOutComponent;
  let fixture: ComponentFixture<AreawiseOutComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AreawiseOutComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AreawiseOutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
