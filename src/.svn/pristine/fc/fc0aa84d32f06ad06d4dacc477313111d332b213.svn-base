import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DuedaywiseOutComponent } from './duedaywise-out.component';

describe('DuedaywiseOutComponent', () => {
  let component: DuedaywiseOutComponent;
  let fixture: ComponentFixture<DuedaywiseOutComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DuedaywiseOutComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DuedaywiseOutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
