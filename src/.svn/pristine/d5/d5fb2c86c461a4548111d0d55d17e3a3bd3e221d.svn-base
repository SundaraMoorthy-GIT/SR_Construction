import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { UserwiseColComponent } from './userwise-col.component';

describe('UserwiseColComponent', () => {
  let component: UserwiseColComponent;
  let fixture: ComponentFixture<UserwiseColComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ UserwiseColComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UserwiseColComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
