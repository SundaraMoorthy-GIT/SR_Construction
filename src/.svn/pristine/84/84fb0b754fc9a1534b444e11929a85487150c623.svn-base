import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddVariableSettingsComponent } from './add-variable-settings.component';

describe('AddVariableSettingsComponent', () => {
  let component: AddVariableSettingsComponent;
  let fixture: ComponentFixture<AddVariableSettingsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddVariableSettingsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddVariableSettingsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
