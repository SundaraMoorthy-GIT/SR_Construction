import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VindropdownComponent } from './vindropdown.component';

describe('VindropdownComponent', () => {
  let component: VindropdownComponent;
  let fixture: ComponentFixture<VindropdownComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VindropdownComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VindropdownComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
