import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerwiseOutComponent } from './customerwise-out.component';

describe('CustomerwiseOutComponent', () => {
  let component: CustomerwiseOutComponent;
  let fixture: ComponentFixture<CustomerwiseOutComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CustomerwiseOutComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CustomerwiseOutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
