import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AreawiseColComponent } from './areawise-col.component';

describe('AreawiseColComponent', () => {
  let component: AreawiseColComponent;
  let fixture: ComponentFixture<AreawiseColComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AreawiseColComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AreawiseColComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
