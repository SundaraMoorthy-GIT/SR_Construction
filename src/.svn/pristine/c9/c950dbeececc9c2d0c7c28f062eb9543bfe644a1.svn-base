import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReferencMasterComponent } from './referenc-master.component';

describe('ReferencMasterComponent', () => {
  let component: ReferencMasterComponent;
  let fixture: ComponentFixture<ReferencMasterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReferencMasterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReferencMasterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
