import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Vinledger1Component } from './vinledger1.component';

describe('Vinledger1Component', () => {
  let component: Vinledger1Component;
  let fixture: ComponentFixture<Vinledger1Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Vinledger1Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Vinledger1Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
