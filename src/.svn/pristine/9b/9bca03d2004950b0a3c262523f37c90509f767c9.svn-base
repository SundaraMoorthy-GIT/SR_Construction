import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AddApprovedListComponent } from './add-approved-list.component';

describe('AddApprovedListComponent', () => {
  let component: AddApprovedListComponent;
  let fixture: ComponentFixture<AddApprovedListComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AddApprovedListComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AddApprovedListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
