import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PageSettupComponent } from './page-settup.component';

describe('PageSettupComponent', () => {
  let component: PageSettupComponent;
  let fixture: ComponentFixture<PageSettupComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PageSettupComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PageSettupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
